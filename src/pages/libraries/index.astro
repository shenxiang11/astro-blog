---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import LibraryCard from "@/components/LibraryCard.astro";
import { getSortedLibraries } from "@/data/libraries";
import { useTranslations } from "@/i18n";
import config from "@/config";

const locale = Astro.currentLocale ?? config.site.lang;
const t = useTranslations(locale);
const items = getSortedLibraries(locale);
---

<Layout
  title={`${t.pages.librariesTitle} | ${config.site.title}`}
  description={t.pages.librariesDesc}
>
  <Header />

  <main id="main-content" class="flex-1">
    <section class="site-layout pt-8 pb-2 text-center sm:pt-12">
      <h1 class="text-4xl font-semibold tracking-tight sm:text-5xl">
        {t.pages.librariesTitle}
      </h1>
      <p class="text-muted-foreground mx-auto mt-5 max-w-2xl text-lg">
        {t.pages.librariesDesc}
      </p>
    </section>

    <section class="site-layout pt-10 pb-24">
      <ul class="mx-auto max-w-4xl">
        {items.map(library => <LibraryCard {library} />)}
      </ul>
    </section>
  </main>

  <Footer />
</Layout>
