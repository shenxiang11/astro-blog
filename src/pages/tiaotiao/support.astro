---
import { getEntry, render } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Breadcrumb from "@/components/Breadcrumb.astro";
import Main from "@/components/Main.astro";
import Footer from "@/components/Footer.astro";
import config from "@/config";

const support = await getEntry("pages", "tiaotiao-support");

if (!support) {
  throw new Error(
    "Missing content entry: `tiaotiao-support.md` in `src/content/pages/`"
  );
}

const { Content } = await render(support);
---

<Layout
  title={`${support.data.title} | ${config.site.title}`}
  description={support.data.description}
  ogImage={support.data.ogImage}
  canonicalURL={support.data.canonicalURL}
>
  <Header />

  <Breadcrumb />

  <Main pageTitle={support.data.title} class="app-prose">
    <Content />
  </Main>

  <Footer />
</Layout>
